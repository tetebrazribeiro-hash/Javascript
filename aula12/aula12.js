// tipo objeto no javascript
//Objeto é uma estrutura que amazena informações em formato de chave: valor

const aluno = {
    nome: "Carlos",
    idade: 22,
    curso: "Sistemas de informação"
};

console.log(aluno.idade);

const Maria = {
    nome: "Maria Eduarda",
    sobrenome: "Gomes",
    matricula: 2103,
    ativo: true
};

if(Maria.ativo) {
    console.log(`Aluno ${Maria.nome} está com a matrícula ativa`);
} else if(nota.ativo) {
    console.log(`Aluna ${Maria.nome} não está com a matrícula ativa`);
}